# 自治系统（AS）与 ASN 基础
> [English](asn.md) · **简体中文**

本文面向初次接触网络互联概念的读者，介绍自治系统与自治系统号的基本知识。本网络（AS64500）的具体情况见 [网络](network.md)。

---

## 什么是自治系统（AS）

互联网不是一张统一的大网，而是由众多各自独立管理的网络互联而成。由同一机构按统一路由策略管理的一组 IP 网络，称为一个自治系统（Autonomous System，AS）。

常见的自治系统包括：互联网服务提供商（ISP）、云服务商、内容分发网络（CDN）、大学以及大型企业等。

## 什么是 ASN

每个自治系统拥有一个全球唯一的编号，称为自治系统号（Autonomous System Number，ASN）。在域间路由协议 BGP 中，ASN 用于标识一条路由来自哪个网络。

ASN 通常写作 `AS` 加数字，例如 `AS64500`。

## ASN 如何分配

ASN 由 IANA 统一管理，逐级分配给五个区域互联网注册管理机构（RIR），再由 RIR 分配给各机构：

| RIR | 覆盖区域 |
|---|---|
| RIPE NCC | 欧洲、中东、中亚 |
| ARIN | 北美 |
| APNIC | 亚太 |
| LACNIC | 拉丁美洲与加勒比 |
| AFRINIC | 非洲 |

## 16 位与 32 位 ASN

- **16 位 ASN**：取值范围 `0`–`65535`，是早期的格式，资源已基本耗尽。
- **32 位 ASN**：取值范围扩展至 `0`–`4294967295`，用于满足后续需求。`AS64500` 即为一个 32 位 ASN。

两种格式在 BGP 中可互通，路由器对其同等处理。

## 公有 ASN 与私有 ASN

- **公有 ASN**：全球唯一，用于在公网上参与 BGP 路由。
- **私有 ASN**：保留供机构内部使用，不在公网宣告。范围为 `64512`–`65534`（16 位）与 `4200000000`–`4294967294`（32 位）。

## ASN 与 BGP 的关系

BGP（边界网关协议）是自治系统之间交换路由信息的协议。每条 BGP 路由都带有一个 **AS_PATH**，即该路由依次经过的 ASN 序列。序列中最初宣告该前缀的自治系统称为 **origin AS**。

接收方可据此判断路由的来源与路径，并结合 RPKI 等机制校验其合法性（参见本网络对 [互联](peering.md) 的要求）。

## 如何查询一个 ASN

公开工具可查询任意 ASN 的宣告前缀、互联关系与注册信息，例如：

- **RIPEstat**（`stat.ripe.net`）
- **bgp.tools**
- **PeeringDB**（`peeringdb.com`）—— 互联信息登记库
- **whois** 查询

---

## 延伸阅读

- [网络](network.md) —— 本网络（AS64500）的拓扑与 anycast 部署
- [互联（Peering）](peering.md) —— 与本网络建立互联的方式
- [Looking Glass](looking-glass.md) —— 实时查看本网络的路由
